import { Router } from 'express';
const router = Router();
// TODO: implement assistantRoutes endpoints
router.get('/', (_req, res) => res.json({ message: 'assistantRoutes stub' }));
export default router;
